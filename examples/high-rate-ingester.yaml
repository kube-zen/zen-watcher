apiVersion: zen.kube-zen.io/v1alpha1
kind: Ingester
metadata:
  name: high-rate-ingester
  namespace: zen-system
spec:
  source: kubernetes-events
  ingester: k8s-events
  
  k8sEvents:
    involvedObjectKinds:
      - Pod
      - Deployment
      - Service
  
  rateLimit:
    enabled: true
    requestsPerSecond: 2000
    burst: 4000
  
  # Processing pipeline configuration (canonical format)
  # Canonical pipeline: source → (filter | dedup) → normalize → destinations[]
  # Conforms to common Ingester semantics (see zen-admin/docs/common/COMMON_INGESTER_CRD_AND_PIPELINE_SEMANTICS_V1.md)
  processing:
    filter:
      enabled: true
      minPriority: 0.3
      excludeNamespaces:
        - kube-system
        - kube-public
    dedup:
      enabled: true
      window: "5m"
      strategy: event-stream  # event-stream for high-volume, noisy sources
      maxEventsPerWindow: 1000  # Optional: strict limit for event-stream
    optimization:
      enabled: true
      order: auto
      autoOptimize: true
      analysisInterval: "15m"
      confidenceThreshold: 0.7
      thresholds:
        dedupEffectiveness:
          warning: 0.5
          critical: 0.3
        lowSeverityPercent:
          warning: 0.7
          critical: 0.9
        observationsPerMinute:
          warning: 100
          critical: 500
  
  # Backward compatibility fields (maps to processing.*)
  # but new examples should use spec.processing.* as the primary form
  filters:
    enabled: true
    minPriority: 0.3
    excludeNamespaces:
      - kube-system
      - kube-public
  deduplication:
    enabled: true
    window: "5m"
    strategy: fingerprint
  
  
  # Destinations: where events go
  destinations:
    # Classic zen-watcher OSS behavior: write Observation CRDs
    - type: crd
      value: observations
      mapping:
        domain: operations
        type: kubernetes_event
        priority:
          Warning: 0.7
          Normal: 0.3
          Error: 0.9
    
    # Optional: send to external webhook with retry policy
    - type: webhook
      name: eu-mirror
      url: "https://eu-hooks.example.com/t/k8s-events"
      retryPolicy:
        maxRetries: 10
        backoff: exponential
        timeout: "30s"
